version: 2

models:
  - name: stg_payments
    description: Staged payments data from our stripe app.
    columns: 
      - name: payments_id
        description: Primary key for payments.
        tests:
          - unique
          - not_null